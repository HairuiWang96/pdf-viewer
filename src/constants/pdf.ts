/** Width (px) of the main PDF page in the viewer */
export const VIEWER_PAGE_WIDTH = 700;

/** Width (px) of thumbnail previews in the sidebar */
export const THUMBNAIL_WIDTH = 140;

/** Initial page shown when a PDF is first opened */
export const INITIAL_PAGE = 1;
